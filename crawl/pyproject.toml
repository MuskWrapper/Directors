[tool.poetry]
name = "crawl"
version = "0.1.0"
description = ""
authors = ["Asher <asher@wrtn.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
